{
    "scripts": {
        "docker-up": "docker compose -f docker-compose.dev.yaml up --build",
        "docker-down": "docker compose -f docker-compose.dev.yaml down"
    },
    "dependencies": {
        "lucide-react": "^0.562.0"
    }
}
